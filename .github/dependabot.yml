version: 2
updates:
  - package-ecosystem: "npm"          # Use your actual package ecosystem (e.g., npm, pip, docker)
    directory: "/"                    # Location of the manifest file
    schedule:
      interval: "weekly"              # How often to check for updates